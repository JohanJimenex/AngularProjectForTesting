import { TestBed } from '@angular/core/testing';

import { MicrosfEntraIdService } from './microsf-entra-id.service';

describe('MicrosfEntraIdService', () => {
  let service: MicrosfEntraIdService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(MicrosfEntraIdService);
  });

});
